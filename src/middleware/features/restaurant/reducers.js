
import * as types from './types'

const initialState = {

}

function restaurant(state = initialState, action) {
    switch (action.type) {
        default:
            return state
    }
}

export default restaurant

