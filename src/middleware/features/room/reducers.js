
import * as types from './types'

const initialState = {

}

function room(state = initialState, action) {
    switch (action.type) {
        default:
            return state
    }
}

export default room

