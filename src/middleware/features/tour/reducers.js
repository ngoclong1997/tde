

import * as types from './types'

const initialState = {

}

function tour(state = initialState, action) {
    switch (action.type) {
        default:
            return state
    }
}

export default tour

