
import * as types from './types'

const initialState = {

}

function place(state = initialState, action) {
    switch (action.type) {
        default:
            return state
    }
}

export default place

