

import * as types from './types'

const initialState = {

}

function hotdeal(state = initialState, action) {
    switch (action.type) {
        default:
            return state
    }
}

export default hotdeal

